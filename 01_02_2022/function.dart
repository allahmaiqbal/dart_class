import 'dart:io';

void main() {
  // int? a = int.parse(stdin.readLineSync()!);
  // int? b = int.parse(stdin.readLineSync()!);
  print(add_myfunction(a, b));
}

int function(a, b) {
  int sum;
  sum = a + b;
  return sum - 1;
}
